public class  linearSearch() {
    public static void main(String[] args) {
        //linear search
        int[] arr = {1,2,3,4};
        int target = 2;
        boolean found = false;
        
        //for loop to check 2 in the main array
        for(int i = 0; i<arr.length; i++){
            if(arr[i] == target);
            found = true;
            break;


        }
        if(found);
        System.out.println(" found");
    }
        else { 
            System.out.println("not found");
        
        
    }
    
}
